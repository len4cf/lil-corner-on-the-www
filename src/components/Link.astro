---
interface Props {
  href: string;
}

const { href, ...props } = Astro.props as Props;
---


<a class="font-baskervville lg:text-[20px] text-[16px] underline underline-offset-4 hover:text-[#73976A] transition-colors" href={href} {...props}>
  <slot />
</a>

